mixin nav(items)
  ul&attributes(attributes)
    each item, key in items
      li(class=item.clsLi)
        +link(item.title, item.href, item.text)(class=item.class)
        - if (typeof(item.itemsMenu) !== 'undefined'){
          +nav(item.itemsMenu.items)(class=item.itemsMenu.clsSub)
        - }
